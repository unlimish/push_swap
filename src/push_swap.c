#include <unistd.h>
#include <stdio.h>
#include "push_swap.h"
#include "libft.h"

int	main(int ac, char **av)
{
	int **a;
	int **b;

	if (ac >= 2)
		return (0);
	av[0]="1";
	check_is_int(ac, av);
	while (*av != "\0")
	{

	}
}
